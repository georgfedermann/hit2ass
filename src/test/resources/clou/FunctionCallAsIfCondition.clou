#
#*  Exposes a bug when using a function call within an If condition expression.     *

#D myVar "Hello, World!"
#? 5 = listlen("myVar"):
  /J
        This should not be printed.
  /N
        This should be printed.
#
And so it ends.
